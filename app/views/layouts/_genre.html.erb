<div class="mx-auto">
  <ul class="list-group">
  <li class="list-group-item active">ジャンル検索</li>
  <% @genres.each do |genre| %>
    <li class="list-group-item list-group-item-action">
      <%= link_to genre.name, search_path('search[value]': genre.id) %>
    </li>
  <% end %>
  </ul>
</div>