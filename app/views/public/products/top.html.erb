<div class="container-fluid text-center">
  <div class="row">
    <div class="mx-auto">
      <h5>
        ようこそ、ながのCAKEへ！<br>
        このサイトは、ケーキ販売のECサイトになります。<br>
        会員でない方も商品の閲覧はできますが、<br>
        購入には会員登録が必要になります。
      </h5>
    </div>
  </div>
</div>
<div class='container-fluid'>
  <div class='row'>
    <div class="mx-auto">
      <ul class="list-group">
      <li class="list-group-product">ジャンル検索</li>
      <% @genres.each do |genre| %>
        <li class="list-group-product">
          <%= link_to genre.name, search_path('search[value]': genre.id) %>
        </li>
      <% end %>
      </ul>
    </div>
    <% @products.first(4).each do |product| %>
      <div class='mx-auto text-center'>
        <div class='card'>
          <%= link_to product_path(product) do %>
            <%= attachment_image_tag(product, :image, size: "250x250", fallback: 'no_image.jpg') %>
            <div class='card-body'>
              <h6 class='card-title'><%= product.name %></h6>
              <p class='card-text'>¥<%= product.tax_excluded_price.to_s(:delimited) %>円(税抜)</p>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
  <div class="text-right">
    <div>
      <%= link_to "商品一覧画面へ", products_path, class: "btn btn-success" %>
    </div>
  </div>
</div>